import en from './en';
import es from './es';

// TODO: only send the translations we need
export default {
	en: en,
	es: es
};

// Currently all files need all the same expressions as found in en.ts
